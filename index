<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Фиджет Сайт</title>
    <style>
        /* Основной стиль страницы */
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background: linear-gradient(to right, #333333, #555555); /* Темно-серый градиент */
            color: white;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
        }

        h1 {
            font-size: 2.5em;
            margin-bottom: 20px;
        }

        .container {
            text-align: center;
        }

        /* Стиль кнопок */
        .btn {
            background-color: #444444;
            border: none;
            color: white;
            padding: 15px 30px;
            text-align: center;
            text-decoration: none;
            display: inline-block;
            font-size: 1.2em;
            margin: 10px;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
        }

        .btn:hover {
            background-color: #666666;
        }

        /* Стиль слайдера */
        .slider-container {
            margin: 20px 0;
        }

        .slider {
            width: 200px;
            height: 15px;
            background: #777777;
            border-radius: 5px;
            appearance: none;
            outline: none;
            transition: background 0.3s ease;
        }

        .slider::-webkit-slider-thumb {
            appearance: none;
            width: 20px;
            height: 20px;
            background: #999999;
            border-radius: 50%;
            cursor: pointer;
            transition: background 0.3s ease;
        }

        .slider:hover {
            background: #666666;
        }

        .slider::-webkit-slider-thumb:hover {
            background: #888888;
        }

        /* Футер */
        footer {
            margin-top: 50px;
            font-size: 1em;
            color: #ccc;
        }
    </style>
</head>
<body>

    <div class="container">
        <h1>Добро пожаловать на фиджет-сайт!</h1>

        <!-- Кнопки -->
        <button class="btn" onclick="alert('Ты нажал на кнопку!')">Нажми меня</button>
        <button class="btn" onclick="alert('Кнопка была нажата!')">Ещё одна кнопка</button>

        <!-- Слайдер -->
        <div class="slider-container">
            <label for="slider">Регулируй слайдер:</label><br>
            <input type="range" id="slider" class="slider" min="1" max="100" value="50">
            <p id="slider-value">Значение: 50</p>
        </div>

        <footer>
            <p>&copy; 2025 Фиджет Сайт. Все права защищены.</p>
        </footer>
    </div>

    <script>
        // Изменение значения слайдера
        const slider = document.getElementById('slider');
        const sliderValue = document.getElementById('slider-value');

        slider.oninput = function() {
            sliderValue.innerHTML = 'Значение: ' + this.value;
        }
    </script>

</body>
</html>
